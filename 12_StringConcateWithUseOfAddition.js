var FirstName = "Krupa";
var LastName = "Patel";

var Name = FirstName + " " + LastName;
console.log("My Name Is " + Name);

/**
 * we can also concate string using += operator
 * we can also appends variable from one to second variable
 * we can also concate variable in output string using plus operator
 */

var mystr;
mystr = "This is the first sentence. "
mystr += "This is the second sentence."
console.log(mystr)